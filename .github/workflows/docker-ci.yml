name: CI/CD Java App with Docker

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build Java project
        run: mvn clean package -DskipTests

      - name: Build Docker image
        run: docker build -t ${{ secrets.arpitdocker123 }}/crud-recipe-app:latest .

      - name: Login to Docker Hub
        run: echo "${{ secrets.arpitdocker123 }}" | docker login -u "${{ secrets.arpitdocker123 }}" --password-stdin

      - name: Push Docker image
        run: docker push ${{ secrets.arpitdocker123 }}/crud-recipe-app:latest
